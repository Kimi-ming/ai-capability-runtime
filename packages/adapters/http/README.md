# HTTP Adapter

The HTTP adapter is the first execution target for OpenCap.

It should support:

- templated URLs
- common HTTP methods
- JSON request bodies
- API key auth through environment variables
- timeout handling
- output validation
