# OpenAPI 适配器

OpenAPI Adapter 是未来能力，用于把 OpenAPI operation 转换成 OpenCap Capability。

目标流程：

```text
OpenAPI operation -> Capability Manifest draft -> validation -> install
```

该 adapter 不属于第一阶段 Runtime 里程碑。
