# OpenAPI Adapter

Planned adapter for converting OpenAPI operations into OpenCap Capabilities.

V1 goal:

```text
OpenAPI operation -> Capability Manifest draft -> validation -> install
```

This adapter is intentionally out of scope for the first runtime milestone.
