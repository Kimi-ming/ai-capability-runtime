{
  "name": "@opencap/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "OpenCap developer CLI.",
  "bin": {
    "opencap": "dist/index.js"
  },
  "exports": {
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm --filter @opencap/spec build && pnpm --filter @opencap/runtime build && pnpm --filter @opencap/mcp build && tsx src/index.ts",
    "test": "pnpm --filter @opencap/spec build && pnpm --filter @opencap/runtime build && pnpm --filter @opencap/mcp build && vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencap/mcp": "workspace:*",
    "@opencap/runtime": "workspace:*",
    "@opencap/spec": "workspace:*",
    "commander": "^12.1.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
